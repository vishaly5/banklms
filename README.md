
  # Frontend Design Only

  This is a code bundle for Frontend Design Only. The original project is available at https://www.figma.com/design/0z4JC2sTq3qdzUnRULLALq/Frontend-Design-Only.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  